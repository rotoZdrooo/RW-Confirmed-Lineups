name: World Cup Lineups Bot

on:
  repository_dispatch:
    types: [check-woc-lineups]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: woc-lineups
  cancel-in-progress: true

jobs:
  run-lineup-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Check for upcoming games
        id: gate
        env:
          ROTOWIRE_KEY_WOC: ${{ secrets.ROTOWIRE_KEY_WOC }}
          CRONJOB_API_KEY: ${{ secrets.CRONJOB_API_KEY }}
          CRONJOB_WC_POLLER_ID: ${{ secrets.CRONJOB_WC_POLLER_ID }}
        run: |
          RESP=$(curl -s "https://api.rotowire.com/Soccer/WOC/Lineups.php?key=$ROTOWIRE_KEY_WOC")
          TODAY=$(TZ=America/New_York date +%Y-%m-%d)
          NOW_EPOCH=$(date -u +%s)

          FUTURE_GAME=false
          for KICKOFF in $(echo "$RESP" | grep -oP "Date=\"${TODAY}T[^\"]+\"" | grep -oP "${TODAY}T[^\"]+"); do
            KICK_EPOCH=$(date -u -d "$KICKOFF" +%s 2>/dev/null || echo "0")
            if [ "$KICK_EPOCH" -gt "$NOW_EPOCH" ]; then
              FUTURE_GAME=true
              break
            fi
          done

          if [ "$FUTURE_GAME" = true ]; then
            echo "World Cup games today that haven't kicked off yet — proceeding."
            echo "has_games=true" >> $GITHUB_OUTPUT
          else
            echo "All games done or no games today — disabling poller."
            echo "has_games=false" >> $GITHUB_OUTPUT
            curl -s -X PATCH \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer $CRONJOB_API_KEY" \
              -d '{"job":{"enabled":false}}' \
              "https://api.cron-job.org/jobs/$CRONJOB_WC_POLLER_ID"
          fi

      - name: Checkout repo
        if: steps.gate.outputs.has_games == 'true'
        uses: actions/checkout@v4.2.2

      - name: Set up Python
        if: steps.gate.outputs.has_games == 'true'
        uses: actions/setup-python@v5.4.0
        with:
          python-version: "3.11"

      - name: Install dependencies
        if: steps.gate.outputs.has_games == 'true'
        run: pip install -r requirements.txt

      - name: Run WOC lineup bot
        if: steps.gate.outputs.has_games == 'true'
        env:
          ROTOWIRE_KEY_WOC: ${{ secrets.ROTOWIRE_KEY_WOC }}
          DISCORD_WEBHOOK_WOC_PREDICTED: ${{ secrets.DISCORD_WEBHOOK_WOC_PREDICTED }}
          DISCORD_WEBHOOK_WOC_CONFIRMED: ${{ secrets.DISCORD_WEBHOOK_WOC_CONFIRMED }}
        run: python woc_lineup_bot.py

      - name: Commit updated state file
        if: steps.gate.outputs.has_games == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add posted_woc_lineups.json
          git diff --cached --quiet || git commit -m "Update posted_woc_lineups.json [skip ci]"
          git push
